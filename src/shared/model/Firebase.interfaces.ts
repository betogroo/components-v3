import { FirebaseError } from 'firebase/app'
import type { DocumentData } from 'firebase/firestore'
import { Timestamp, Query } from 'firebase/firestore'

export { DocumentData, Timestamp, Query, FirebaseError }
